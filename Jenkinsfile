pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               echo "Hello World"
                bat "ipconfig"
               echo "Multiline shell steps works too"
                bat "dir"
                
            }
        }
    }
}
